This is a CLI to-do list app project. 

This application provides a menu for option selction.

Once options have been selection you can execute functions to add tasks, view tasks or delete tasks.  You can also quit the app from the menu

The application contains exception and error handling for invalid responses and missing data. 
